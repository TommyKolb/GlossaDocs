o
  # Document editor UI

  This is a code bundle for Document editor UI. The original project is available at https://www.figma.com/design/mZ0s7MhXYVfUrNnnhmmWyk/Document-editor-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  